/// @function scr_check_bounds(arr, ind)
/// @description scr_check_bounds Check to see if an index is valid in an array
/// @param arr Array to test
/// @param ind Index to test

return argument1 >= 0 && argument1 < array_length_1d(argument0);
